#include <bits/stdc++.h>
using namespace std;

#define N 300005
int a[N], stk1[N], top1, stk2[N], top2;
int main() {
    int n, i; long long ans = 0;
    cin >> n;
    for (i = 1; i <= n; ++i) {
        cin >> a[i];
    }
    for (i = 1; i <= n; ++i) {
        ans += a[i] / 2;
        if ()
    }
}